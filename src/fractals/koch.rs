use image::{
	Rgba,
};
use imageproc::drawing::{
	draw_polygon,
};

use crate::util;

/// Rendering a Curve fractal, the Koch Curve
pub fn render(buf: &mut util::RgbaBuffer, width: u32, height: u32) -> Result<(), image::ImageError> {
	Ok(())
}
